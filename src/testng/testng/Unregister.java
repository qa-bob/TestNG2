package testng;

import org.openqa.selenium.WebDriver;

public class Unregister {
    public static void unreg (WebDriver driver, int secs) {
    	boolean testResult = true;
        System.out.println("This class will unregister a policy");
    }
}
